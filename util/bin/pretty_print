#!/bin/bash

OLD_DIR="$(pwd)"
cd "$(dirname "$0")"/../..
. util/bin/bare_repo_setup
FILE="$(resolve_path "$OLD_DIR" ${1+"$@"})"
FILE_TYPE="$(get_file_type "$FILE")"
run_all_scripts "util/helpers/pretty_print.d/$FILE_TYPE/" "$FILE"
